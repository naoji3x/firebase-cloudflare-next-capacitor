pre-commit:
  parallel: true
  commands:
    format-frontend:
      root: frontend
      glob: "*.{js,jsx,ts,tsx,md,mdx,html,css,scss,json,yml}"
      run: yarn prettier {staged_files} --write
    lint-frontend:
      root: frontend
      run: yarn lint:fix
